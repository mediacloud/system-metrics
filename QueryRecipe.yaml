vars:
    - QUERY
    - COLLECTIONS
    - NAME
steps:
    - QueryOnlineNews:
        params:
            date_mode: "daily"
            window_size: 60
            query: $QUERY
            collections: $COLLECTIONS
