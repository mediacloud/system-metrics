vars:
    - QUERY
    - COLLECTIONS
    - NAME
    - STAGING
steps:
    - QueryOnlineNews:
        params:
            use_staging: $STAGING
            date_mode: "daily"
            window_size: 60
            query: $QUERY
            collections: $COLLECTIONS

    - CountOverTime:
        params:
            use_staging: $STAGING
            date_mode: "daily"
            window_size: 60
            query: $QUERY
            collections: $COLLECTIONS
            timeout_secs: 6000